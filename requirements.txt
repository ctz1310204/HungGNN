# Requirements for HungGNN (GNN_LSAP)
# Linear Sum Assignment Problem using Graph Neural Network
# Compatible with Python 3.9-3.12

# PyTorch 2.x (torch 1.x no longer available on PyPI)
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# PyTorch Geometric (automatically handles torch-scatter, torch-sparse, etc.)
torch-geometric>=2.4.0

# Core dependencies
numpy>=1.21.0
scipy>=1.9.0
pandas>=1.3.0

# Visualization & Logging
tensorboard>=2.8.0
tqdm>=4.62.0

# Data processing (optional)
matplotlib>=3.5.0
seaborn>=0.11.0

# ====== INSTALLATION INSTRUCTIONS ======

# For LOCAL / Standard environments:
#   pip install -r requirements.txt

# For GOOGLE COLAB (recommended - most packages pre-installed):
#   !pip install torch-geometric scipy pandas tqdm

# Verify installation:
#   python -c "import torch; print(f'PyTorch: {torch.__version__}')"
#   python -c "import torch_geometric; print(f'PyG: {torch_geometric.__version__}')"

# Note: Code is compatible with both PyTorch 1.x and 2.x
