# Requirements for HungGNN (GNN_LSAP)
# Linear Sum Assignment Problem using Graph Neural Network
# Compatible with Python 3.8-3.11

# PyTorch and PyTorch Geometric
# Note: torch 1.11.0 was yanked, using 1.13.1 (stable, widely compatible)
torch>=1.13.0,<2.0.0
torch-geometric>=2.3.0

# Core dependencies
numpy>=1.21.0,<2.0.0
scipy>=1.9.0,<2.0.0  # 1.8.1 doesn't exist, using 1.9.0+
pandas>=1.3.0,<2.0.0

# Visualization & Logging
tensorboard>=2.8.0
tqdm>=4.62.0

# Data processing (optional)
matplotlib>=3.5.0
seaborn>=0.11.0

# ====== INSTALLATION INSTRUCTIONS ======

# For LOCAL / Standard environments:
#   pip install -r requirements.txt

# For GOOGLE COLAB (recommended):
#   !pip install torch torchvision torchaudio
#   !pip install torch-geometric
#   !pip install scipy pandas tqdm tensorboard matplotlib seaborn

# For PyTorch Geometric with CUDA (optional, for GPU acceleration):
#   !pip install pyg-lib torch-scatter torch-sparse torch-cluster -f https://data.pyg.org/whl/torch-2.0.0+cu118.html

# Verify installation:
#   python -c "import torch; print(f'PyTorch: {torch.__version__}')"
#   python -c "import torch_geometric; print(f'PyG: {torch_geometric.__version__}')"
